import 'package:flutter/cupertino.dart';

class transcations {
  String id;
  String title;
  double money;
  DateTime date;

  transcations({this.id,
     @required this.title,
     @required this.money,
     @required this.date});
}
